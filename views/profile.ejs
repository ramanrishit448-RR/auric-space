<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width,initial-scale=1,viewport-fit=cover"
    />

    <script src="https://cdn.tailwindcss.com"></script>

    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap"
      rel="stylesheet"
    />

    <title>Profile | MyApp — Luxe</title>

    <style>
      :root {
        --bg-900: #0b0b0c;
        --bg-800: #111216;
        --card: rgba(255, 255, 255, 0.03);
        --glass-border: rgba(255, 255, 255, 0.05);
        --gold: #d4af37;
        --muted: #9aa0a6;
        --accent-glow: rgba(212, 175, 55, 0.12);
      }

      html,
      body {
        height: 100%;
      }

      body {
        font-family: "Poppins", system-ui, -apple-system, "Segoe UI", Roboto,
          "Helvetica Neue", Arial;
        background: linear-gradient(
          180deg,
          var(--bg-900) 0%,
          var(--bg-800) 100%
        );
        color: #e6e6e6;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      .vignette::after {
        content: "";
        position: absolute;
        inset: 0;
        pointer-events: none;
        background: radial-gradient(
            1000px 400px at 10% 10%,
            rgba(255, 255, 255, 0.02),
            transparent 20%
          ),
          radial-gradient(
            800px 300px at 90% 90%,
            rgba(0, 0, 0, 0.25),
            transparent 30%
          );
        mix-blend-mode: overlay;
        opacity: 0.9;
      }

      .glass {
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.02),
          rgba(255, 255, 255, 0.01)
        );
        border: 1px solid var(--glass-border);
        backdrop-filter: blur(8px) saturate(120%);
        -webkit-backdrop-filter: blur(8px) saturate(120%);
      }

      .accent-underline {
        background: linear-gradient(
          90deg,
          transparent,
          var(--gold),
          transparent
        );
        height: 3px;
        border-radius: 2px;
        display: block;
      }

      .btn-anim {
        transition: transform 180ms ease, box-shadow 200ms ease,
          opacity 150ms ease;
      }
      .btn-anim:hover {
        transform: translateY(-4px) scale(1.01);
        box-shadow: 0 8px 30px var(--accent-glow);
      }

      .fade-in {
        animation: fadeIn 420ms ease both;
      }
      @keyframes fadeIn {
        from {
          transform: translateY(8px);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }

      .gold-corner::before {
        content: "";
        position: absolute;
        top: -18px;
        left: -18px;
        width: 52px;
        height: 52px;
        background: conic-gradient(
          from 180deg at 50% 50%,
          transparent,
          transparent 50%,
          var(--gold) 50%
        );
        opacity: 0.06;
        filter: blur(4px);
        transform: rotate(25deg);
        pointer-events: none;
      }

      ::-webkit-scrollbar {
        width: 10px;
      }
      ::-webkit-scrollbar-thumb {
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.06),
          rgba(255, 255, 255, 0.03)
        );
        border-radius: 999px;
        border: 2px solid transparent;
        background-clip: padding-box;
      }
    </style>
  </head>

  <body class="min-h-screen relative vignette">
    <header class="w-full glass border-b var(--glass-border)">
      <div
        class="max-w-6xl mx-auto px-6 md:px-8 lg:px-12 py-5 flex items-center justify-between"
      >
        <div class="flex items-center gap-4">
          <div class="relative">
            <img
              src="/logo.png"
              alt="Logo"
              class="h-12 w-12 rounded-lg object-cover shadow-sm"
            />

            <span
              class="absolute -bottom-1 -right-1 block w-3 h-3 rounded-full"
              style="
                background: var(--gold);
                box-shadow: 0 6px 18px var(--accent-glow);
                opacity: 0.95;
              "
            ></span>
          </div>
          <div>
            <div class="text-lg font-semibold tracking-wide">MyApp</div>
            <div class="text-xs text-zinc-400">Luxury Tech Dashboard</div>
          </div>
        </div>

        <nav class="flex items-center gap-3">
          <a
            href="/"
            class="text-sm text-zinc-300 hover:text-white px-3 py-2 rounded-md btn-anim transition"
            >Home</a
          >

          <a
            href="/profile"
            class="text-sm px-3 py-2 rounded-md font-medium"
            style="color: var(--gold)"
            >Profile</a
          >

          <a
            href="/logout"
            class="ml-4 px-4 py-2 rounded-md glass btn-anim text-sm"
            style="border: 1px solid rgba(255, 255, 255, 0.04)"
            >Logout</a
          >
        </nav>
      </div>
      <div class="max-w-6xl mx-auto px-6 md:px-8 lg:px-12">
        <span class="block w-20 mt-1 accent-underline"></span>
      </div>
    </header>

    <main class="max-w-6xl mx-auto px-6 md:px-8 lg:px-12 pt-10 pb-20">
      <!-- Hero / Welcome -->
      <section class="grid grid-cols-1 md:grid-cols-3 gap-8 items-start mb-8">
        <div
          class="md:col-span-2 glass rounded-2xl p-7 gold-corner relative fade-in"
        >
          <div class="flex items-start gap-6">
            <div class="flex-shrink-0">
              <div
                class="h-20 w-20 rounded-xl overflow-hidden border border-zinc-800"
              >
                <img
                  src="<%= user.avatar ? user.avatar : 'https://tse4.mm.bing.net/th/id/OIP.fAlWDpfABD09s6qnGYN0jgHaHa?pid=Api&P=0&h=180' %>"
                  alt="Avatar"
                  class="h-full w-full object-cover"
                />
              </div>
              <form action="/profile/avatar" method="post" enctype="multipart/form-data" class="mt-2">
                <label class="text-xs text-zinc-400">Change avatar</label>
                <div class="flex items-center gap-2 mt-1">
                  <input id="avatar-input" type="file" name="avatar" accept="image/*" class="hidden" />
                  <label for="avatar-input" class="px-2 py-1 rounded-md glass btn-anim text-xs cursor-pointer" style="border: 1px solid rgba(255,255,255,0.06)">Choose file</label>
                  <span id="avatar-filename" class="text-xs text-zinc-400 truncate max-w-[160px]">No file chosen</span>
                  <button type="submit" class="px-2 py-1 rounded-md glass btn-anim text-xs" style="border: 1px solid rgba(255,255,255,0.06)">Upload</button>
                </div>
              </form>
            </div>

            <div class="flex-1">
              <h1
                class="text-2xl md:text-3xl font-extrabold tracking-tight leading-tight"
              >
                Welcome back,
                <span class="text-gold" style="color: var(--gold)"
                  ><%= user.name %></span
                >
              </h1>
              <p class="mt-2 text-sm text-zinc-400 max-w-2xl">
                Your private space — share ideas, manage posts, and control your
                presence. This theme emphasizes quiet luxury and focus.
              </p>


  
              </div>
            </div>
          </div>
        </div>

        <aside class="glass rounded-2xl p-6 fade-in">
          <div class="flex items-center justify-between">
            <div>
              <div class="text-xs text-zinc-400 uppercase">Posts</div>
              <div class="mt-2 text-3xl font-extrabold">
                <%= user.posts.length %>
              </div>
            </div>
            <div class="text-right">
              <div class="text-xs text-zinc-400">Likes</div>
              <div class="mt-2 text-2xl font-semibold text-zinc-100">
                <%= user.posts.reduce((acc,p)=> acc + (p.likes?
                p.likes.length:0), 0) %>
              </div>
            </div>
          </div>

          <div class="mt-6 grid grid-cols-2 gap-3">
            <div class="p-3 rounded-md glass text-center">
              <div class="text-xs text-zinc-400">Active</div>
              <div class="mt-1 font-semibold">7d</div>
            </div>
            <div class="p-3 rounded-md glass text-center">
              <div class="text-xs text-zinc-400">Engagement</div>
              <div class="mt-1 font-semibold">+12%</div>
            </div>
          </div>
        </aside>
      </section>

      <section class="mb-12">
        <div class="glass rounded-2xl p-6 fade-in">
          <h2 class="text-lg font-semibold mb-3">Create a New Post</h2>

           <form action="/post" method="post" enctype="multipart/form-data" class="space-y-4">
            <input
              type="text"
              name="title"
              placeholder="Title"
              class="w-full p-3 rounded-md bg-transparent border border-zinc-800 placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-opacity-60 focus:ring-gold"
              required
            />

            <textarea
              name="content"
              rows="4"
              placeholder="Write something thoughtful..."
              class="w-full p-3 rounded-md bg-transparent border border-zinc-800 placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-opacity-60 focus:ring-gold resize-none"
              required
            ></textarea>

             <div>
               <label class="block text-sm text-zinc-400 mb-1">Image (optional)</label>
               <input type="file" name="image" accept="image/*" class="block w-full text-sm text-zinc-300 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-zinc-800 file:text-zinc-200 hover:file:bg-zinc-700" />
             </div>

            <div class="flex items-center justify-between">
              <div class="text-sm text-zinc-500">Audience: Private</div>
              <div class="flex items-center gap-3">
                <button
                  type="submit"
                  class="px-5 py-2 rounded-md btn-anim"
                  style="
                    background: linear-gradient(
                      90deg,
                      var(--gold),
                      rgba(255, 255, 255, 0.02)
                    );
                    color: #0b0b0c;
                    font-weight: 700;
                  "
                >
                  Publish
                </button>
                <a
                  href="#"
                  class="text-sm text-zinc-400 hover:text-zinc-200 transition"
                  >Preview</a
                >
              </div>
            </div>
          </form>
        </div>
      </section>

      <section>
        <h3 class="text-xl font-semibold mb-4">Your Posts</h3>

        <div class="grid gap-6">
          <% if (user.posts.length === 0) { %>
          <div class="glass rounded-2xl p-6 text-center text-zinc-400">
            No posts yet. Create your first one.
          </div>
          <% } else { %> <% user.posts.reverse().forEach(function(post) { %>
          <article
            class="glass rounded-2xl p-6 fade-in relative overflow-hidden"
          >
            <div class="flex items-start justify-between gap-4">
               <div>
                <h4 class="text-lg font-semibold"><%= post.title %></h4>
                <p class="mt-2 text-sm text-zinc-300 leading-relaxed">
                  <%= post.content %>
                </p>
                 <% if (post.image) { %>
                 <div class="mt-3">
                   <img src="<%= post.image %>" alt="post image" class="rounded-lg border border-zinc-800 max-h-80 object-cover" />
                 </div>
                 <% } %>
              </div>

              <div class="flex flex-col items-end gap-3">
                <form action="/post/<%= post._id %>/like" method="post">
                  <button
                    type="submit"
                    class="px-3 py-2 rounded-md btn-anim text-sm"
                    style="
                      background: linear-gradient(
                        180deg,
                        rgba(212, 175, 55, 0.07),
                        rgba(212, 175, 55, 0.03)
                      );
                      border: 1px solid rgba(212, 175, 55, 0.06);
                      color: var(--gold);
                    "
                  >
                    Like (<%= post.likes ? post.likes.length : 0 %>)
                  </button>
                </form>

                <div class="flex gap-2">
                  <a
                    href="/post/<%= post._id %>/edit"
                    class="px-3 py-2 rounded-md glass btn-anim text-sm"
                    style="border: 1px solid rgba(255, 255, 255, 0.04)"
                    >Edit</a
                  >
                  <form
                    action="/post/<%= post._id %>/delete"
                    method="post"
                    onsubmit="return confirm('Delete this post?');"
                  >
                    <button
                      type="submit"
                      class="px-3 py-2 rounded-md btn-anim text-sm"
                      style="
                        background: rgba(255, 30, 60, 0.06);
                        border: 1px solid rgba(255, 30, 60, 0.06);
                        color: #ff6b6b;
                      "
                    >
                      Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>

            <!-- subtle gold glow line -->
            <div
              class="absolute left-6 bottom-6 w-28 h-1 rounded-full"
              style="
                background: linear-gradient(
                  90deg,
                  transparent,
                  rgba(212, 175, 55, 0.15),
                  transparent
                );
              "
            ></div>
          </article>
          <% }); %> <% } %>
        </div>
      </section>
    </main>
    <script>
      (function () {
        const input = document.getElementById('avatar-input');
        const nameSpan = document.getElementById('avatar-filename');
        if (!input || !nameSpan) return;
        input.addEventListener('change', function () {
          if (this.files && this.files[0]) {
            nameSpan.textContent = this.files[0].name;
          } else {
            nameSpan.textContent = 'No file chosen';
          }
        });
      })();
    </script>
  </body>
</html>
