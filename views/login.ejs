<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login | MyApp</title>
    <style>
      @keyframes gradientMove {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }
      body {
        background: linear-gradient(-45deg, #0f172a, #0e7490, #1e3a8a, #0f172a);
        background-size: 300% 300%;
        animation: gradientMove 10s ease infinite;
      }
    </style>
  </head>
  <body
    class="min-h-screen flex items-center justify-center text-white font-sans"
  >
    <div
      class="w-full max-w-md p-8 rounded-2xl bg-zinc-900/70 backdrop-blur-lg border border-zinc-700 shadow-[0_0_30px_rgba(0,255,255,0.15)] hover:shadow-[0_0_45px_rgba(0,255,255,0.25)] transition-all duration-500"
    >
      <!-- Logo -->
      <div class="flex justify-center mb-6">
        <img
          src="/logo.png"
          alt="Logo"
          class="h-12 w-12 rounded-full shadow-[0_0_20px_rgba(0,255,255,0.4)]"
        />
      </div>

      <h1
        class="text-4xl font-extrabold text-center mb-6 text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500"
      >
        Welcome Back
      </h1>

      <form action="/login" method="POST" class="flex flex-col gap-5">
        <input
          type="email"
          name="email"
          placeholder="Email"
          class="p-3 rounded-lg bg-zinc-800 border border-zinc-700 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-500 focus:outline-none transition-all"
          required
        />
        <input
          type="password"
          name="password"
          placeholder="Password"
          class="p-3 rounded-lg bg-zinc-800 border border-zinc-700 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-500 focus:outline-none transition-all"
          required
        />

        <button
          type="submit"
          class="bg-gradient-to-r from-cyan-500 to-blue-500 hover:from-cyan-400 hover:to-blue-400 font-semibold py-3 rounded-lg transition-all hover:scale-[1.02] shadow-[0_0_20px_rgba(0,255,255,0.3)]"
        >
          Login
        </button>
      </form>

      <p class="text-center mt-6 text-zinc-400">
        Don’t have an account?
        <a
          href="/"
          class="text-cyan-400 hover:text-cyan-300 hover:underline font-semibold"
          >Register here</a
        >
      </p>
    </div>
  </body>
</html>
